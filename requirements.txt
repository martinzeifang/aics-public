# Web-Framework
Flask>=3.0.0,<4.0.0
Flask-JWT-Extended>=4.6.0,<4.7.0  # 4.7 Breaking: Subject must be string
Flask-CORS>=6.0.0,<7.0.0  # CVE-2024-6839/6844/6866 fixes
Flask-Limiter>=3.5.0,<4.0.0

# Auth + Crypto (CVE-Fixes #274)
cryptography>=43.0.0,<49.0.0  # CVE-2024-26130, CVE-2024-12797 fixes; Upper-Bound gegen Überraschungs-Majors (#745)
werkzeug>=3.1.8,<4.0.0  # CVE-2024-49767, CVE-2024-49766 fixes
pyjwt>=2.12.0,<3.0.0  # GHSA-752w-5fwx-jx9f (transitiv via Flask-JWT-Extended)

# DB
sqlalchemy>=2.0.49,<3.0.0
# PostgreSQL (Migration #15) — psycopg3 + Connection-Pool
psycopg[binary]>=3.1,<4.0
psycopg-pool>=3.2,<4.0

# In-App-Scheduler (C3 Vulnerability-Sync, #949)
apscheduler>=3.10.4,<4.0.0

# Gutachten Custom-Word-Vorlagen (#957)
docxtpl>=0.16,<1.0

# Document Processing
pillow>=10.3.0,<13.0.0  # CVE-2024-28219, CVE-2023-50447 fixes; Upper-Bound gegen Überraschungs-Majors (#745)
openpyxl>=3.1.5
python-docx>=1.1.2

# HTTP (CVE-Fix #275)
requests>=2.32.3
urllib3>=2.7.0,<3.0.0  # CVE-2024-37891 fix

# YAML (CVE-Fix #276)
PyYAML>=6.0.3  # CVE-2020-14343, CVE-2017-18342 fixes

# Reports (PDF)
reportlab>=4.2.0,<5.0.0

# API Documentation
flasgger>=0.9.7,<1.0.0

# 2FA (TOTP + QR-Code-Rendering)
pyotp>=2.9.0,<3.0.0
qrcode>=7.4.2,<9.0.0

# Passkey / WebAuthn / FIDO2 (Sprint ε)
webauthn>=2.0.0,<3.0.0

# Optional: LDAP/AD-Authentifizierung (server/auth/ldap.py).
# Modul ist ohne ldap3 funktionsfähig (Fallback). Für echten LDAP-Login
# installieren: pip install "ldap3>=2.9,<3.0"
# ldap3>=2.9,<3.0

# Utilities
python-dateutil>=2.9.0

# ---- TODO (#745, DEFERRED) -------------------------------------------------
# Vollständiger gehashter Lockfile via pip-compile (pip-tools) als
# requirements.lock — bewusst auf Folge-PR verschoben, um CI-Dependency-Churn
# in diesem PR zu vermeiden.
# ----------------------------------------------------------------------------

# ---- Test-Dependencies (separat in requirements-dev.txt sinnvoller, hier inline) ----
# pytest>=8.0.0
# pytest-flask>=1.3.0
# pytest-cov>=5.0.0
