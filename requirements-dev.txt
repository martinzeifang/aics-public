# Development + Test Dependencies
-r requirements.txt

pytest>=8.0.0,<10.0.0
pytest-flask>=1.3.0,<2.0.0
pytest-cov>=5.0.0,<8.0.0

# Postgres-Tests (#1341): ephemerer Postgres-Container für lokale Tests (Win+Linux)
testcontainers[postgresql]>=4.14.2,<5.0.0

# SAST (#736)
bandit>=1.8,<2.0

# Linting / Formatting (optional)
# ruff>=0.6.0
# mypy>=1.10.0

# Eingebettetes Dev-PostgreSQL (Docker-frei, lokaler Test Win+Linux) — #15
pgserver>=0.1.4
